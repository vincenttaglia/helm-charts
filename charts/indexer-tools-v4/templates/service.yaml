apiVersion: v1
kind: Service
metadata:
  name: {{ include "indexer-tools-v4.fullname" . }}
  labels:
    {{- include "indexer-tools-v4.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "indexer-tools-v4.selectorLabels" . | nindent 4 }}
